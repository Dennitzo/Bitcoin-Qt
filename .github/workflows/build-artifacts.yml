name: Build Artifacts

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

env:
  BUILD_TYPE: Release

jobs:
  build:
    name: ${{ matrix.label }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS ARM64
            runner: macos-14
            qt-version: "6.7.3"
            qt-host: mac
            qt-arch: clang_64
            build-runtime: true
            artifact: bitcoin-qt-macos-arm64
          - label: Linux x64
            runner: ubuntu-24.04
            qt-version: "6.7.3"
            qt-host: linux
            qt-arch: linux_gcc_64
            build-runtime: true
            artifact: bitcoin-qt-linux-x64
          - label: Windows x64
            runner: windows-2022
            qt-version: "6.10.3"
            qt-host: windows
            qt-arch: win64_msvc2022_64
            msvc-arch: x64
            build-runtime: true
            artifact: bitcoin-qt-windows-x64

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install system dependencies
        if: runner.os != 'Windows'
        run: ./scripts/ci-install-deps.sh

      - name: Install Windows system dependencies
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install llvm -y --no-progress
          rustup default stable-x86_64-pc-windows-msvc
          rustup target add x86_64-pc-windows-msvc

      - name: Set up MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.msvc-arch }}

      - name: Install Qt
        if: runner.os != 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ matrix.qt-version }}
          host: ${{ matrix.qt-host }}
          target: desktop
          arch: ${{ matrix.qt-arch }}
          modules: qtwebengine qtwebchannel qtpositioning
          cache: true

      - name: Install Qt for Windows
        if: runner.os == 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          dir: C:\Qt
          version: ${{ matrix.qt-version }}
          host: ${{ matrix.qt-host }}
          target: desktop
          arch: ${{ matrix.qt-arch }}
          modules: qtwebengine qtwebchannel qtpositioning
          cache: true

      - name: Build runtime
        if: matrix.build-runtime == true && runner.os != 'Windows'
        run: ./scripts/build-runtime.sh

      - name: Build Windows runtime
        if: matrix.build-runtime == true && runner.os == 'Windows'
        shell: pwsh
        run: .\windows\stage-runtime.ps1 -BuildElectrs

      - name: Build
        if: runner.os != 'Windows'
        run: ./scripts/build-app.sh

      - name: Build Windows app
        if: runner.os == 'Windows'
        shell: pwsh
        run: .\windows\build-app.ps1 -Configuration $env:BUILD_TYPE -QtRoot C:\Qt

      - name: Package
        if: runner.os != 'Windows'
        env:
          ARTIFACT_NAME: ${{ matrix.artifact }}
        run: ./scripts/package-artifact.sh

      - name: Package Windows artifact
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          ARTIFACT_NAME: ${{ matrix.artifact }}
        run: .\windows\package-artifact.ps1 -Configuration $env:BUILD_TYPE

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            build/dist/${{ matrix.artifact }}.tar.gz
            build/dist/${{ matrix.artifact }}.zip
          if-no-files-found: error
